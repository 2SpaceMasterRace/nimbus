[project]
name = "nimbus-runtime"
version = "0.1.0"
description = "Shared Nimbus chat/runtime orchestration for ai_server and future adapters"
requires-python = ">=3.12"
dependencies = [
    "ai-client-api",
    "cloud-storage-api",
    "nimbus-protocol",
    "fastapi>=0.115.0",
    "logfire[httpx]>=4.32.0",
    "opentelemetry-exporter-otlp-proto-http>=1.39.1",
    "opentelemetry-instrumentation-fastapi>=0.60b1",
    "opentelemetry-instrumentation-httpx>=0.60b1",
    "opentelemetry-sdk>=1.39.1",
    "psycopg[binary]>=3.2.0",
    "pydantic>=2.9.0",
    "sentry-sdk>=2.58.0",
    "structlog>=25.5.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["nimbus_runtime"]

[tool.uv.sources]
ai-client-api = { workspace = true }
nimbus-protocol = { workspace = true }
cloud-storage-api = { git = "https://github.com/2SpaceMasterRace/ospsd-cloud-storage.git", rev = "v1.0.0" }
