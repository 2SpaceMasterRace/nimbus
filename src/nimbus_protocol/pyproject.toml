[project]
name = "nimbus-protocol"
version = "0.1.0"
description = "Shared Nimbus protocol DTOs for CLI, runtime, HTTP, and chat adapters"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["nimbus_protocol"]
