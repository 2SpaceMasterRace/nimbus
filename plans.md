#+title: plans.md
#+subtitle: Inspired by John Carmack's .plan files

* Prefix legend
  * DONE  completed
  * TODO  not yet started
  * NOW   in progress
  * HOLD  blocked / on ice
  * KILL  decided against

  Create a damn simple website to show it in action and have a pitch for your YC nomination:
an AI agent for unified cloud storage where you can chat naturally with your AWS, GCP, Azure, or Dropbox files right from the terminal, or connect it to Slack (and any other chat platform) to instantly backup and manage everything.

------------------------------------------------------------
 migrate to buildkite, launchdarkly, temporal, Also add to observability tasks : run tests, review telemetry, and query feature flags.
integrate cloudflare and other protection against the publicly deployed servers
ngrok too

* TODO Now [2/20]
** DONE Documentation [3/8]
   * DONE Setup Sphinx similar to attrs documentation
   * DONE Add proper git-workflow (assignment-amend) to CONTRIBUTION.md
   * DONE Write testing.md — comprehensive testing guide: all five test categories, how each works, fuzz harness setup, how to write your own tests (added to Sphinx toctree)
   * TODO Write a proper MkDocs / Sphinx site [0/3]
     * TODO Quickstart guide
     * TODO Architecture overview explaining interface/implementation separation
     * TODO API reference docs auto-generated from docstrings
   * TODO Add sphinx-multiversion to host multiple doc versions simultaneously
   * TODO Add doctest integration — examples in docstrings are executable tests in CI
   * TODO Enforce Google-style docstrings via ruff D-rules (pydocstyle)
   * TODO Add CHANGELOG.md generated by Towncrier [0/3]
     * TODO Every PR adds a news fragment in changelog.d/
     * TODO Release cuts the full changelog automatically
     * TODO Ties directly into the Nightly → Beta → Stable release train
   * TODO Write governance and policy docs [0/3]
     * TODO SECURITY.md — responsible disclosure policy
     * TODO GOVERNANCE.md — who can merge, who can release, how decisions get made
     * TODO CITATION.cff — for academic use

** DONE Tooling & Codebase Hygiene [3/9]
   * DONE Add structured logging via structlog [0/2]
     * DONE JSON log lines with bound context (request ID, bucket name, operation type)
     * DONE Every log line machine-parseable and correlatable with OTel traces by request ID
   * DONE Setup CircleCI [0/6]
     * DONE Linting (ruff)
     * DONE Type checking (mypy --strict)
     * DONE Tests + coverage reporting (80% threshold, branch coverage enabled)
     * DONE pre-commit run --all-files as a fast gate before slow test jobs
     * DONE property-tests CI job — Hypothesis tests with coverage contribution, wired into coverage-gate
     * DONE fuzz-smoke CI job — all three harnesses in PYTHONFUZZ_NO_ATHERIS=1 mode on every push
   * TODO Setup pre-commit hooks [0/6]
     * TODO trailing-whitespace, end-of-file-fixer, check-yaml, check-toml, check-ast
     * TODO check-merge-conflict, debug-statements (from pre-commit-hooks)
     * TODO pyupgrade --py312-plus (auto-upgrades syntax: Union[x,y] → x|y, etc.)
     * TODO add-trailing-comma (cleaner diffs when adding function args)
     * TODO ruff-check --fix + ruff-format
     * TODO mypy --strict with pass_filenames: false (catches cross-file type errors)
   * DONE Add py.typed marker file to signal typed package to downstream consumers
   * TODO Add nox for task automation (Sprint 5 Block H) [0/9]
     * TODO noxfile.py with sessions: lint, test, property, mutate, docs, schemathesis, load, bdd, security
     * TODO nox -s test -- pytest src/ -m "unit or regression"
     * TODO nox -s property -- pytest -m property
     * TODO nox -s mutate -- mutmut run on auth/sessions/conversation/state_store
     * TODO nox -s schemathesis -- run TestClient(app) + schemathesis run
     * TODO nox -s load -- locust headless run against /ai/chat/turn
     * TODO nox -s bdd -- pytest tests/bdd
     * TODO Decision: skip tox (declarative INI not earning its keep when uv exists)
     * TODO Decision: skip poethepoet (overlaps with nox+just; would create three task runners)
   * TODO Setup justfile as single canonical entry point for all dev commands (Sprint 5 Block H) [1/2]
     * DONE just setup, just test, just lint, just nimbus, just smoke-wrapper — new contributor needs nothing else
     * TODO install, format, test, lint, docs, security targets all covered
   * TODO Look into Graphite for the team — stacked diffs and PR workflow
   * TODO Setup issue triage workflow similar to Ghostty [0/3]
     * TODO CODEOWNERS — specific files owned by specific people, auto-assigned as reviewers
     * TODO Issue templates — bug report, feature request, structured for fast triage
     * TODO github-labeler — auto-tags PRs by files changed
   * TODO Add Hurl for HTTP request testing and CI integration [0/4]
     * TODO Write .hurl files for every FastAPI endpoint (upload, download, list, delete)
     * TODO Run hurl --test in CI as a lightweight integration smoke layer
     * TODO Use Hurl for contract-level testing against the live Fly.io service
     * TODO Replace manual curl one-liners in CONTRIBUTING.md with runnable .hurl files

** TODO Testing [10/24]
   * DONE Look into mocks for testing (moto for AWS)
   * TODO Write genuinely good tests with realistic mocking via moto [0/2]
     * TODO unit, integration, and property-based test tiers
     * TODO meaningful coverage threshold (90%+) enforced in CI
   * DONE Setup Hypothesis for property-based testing [3/3]
     * DONE 38 property tests across 4 files: test_conversation_properties.py, test_session_properties.py, test_auth_properties.py, test_router_properties.py
     * DONE Stateful machine testing (ConversationMachine with RuleBasedStateMachine — bounded-history, orphan-TOOL, round-trip invariants)
     * DONE Integrated into CI as dedicated property-tests job with coverage contribution
   * DONE Setup pytest markers: unit, integration, property, regression, e2e
   * DONE Coverage-guided fuzz testing with Atheris (Google) [3/3]
     * DONE fuzz_conversation.py — Conversation.from_json + _message_from_dict (209 smoke inputs)
     * DONE fuzz_session_id.py — _validate_session_id + _session_file_stem, path-separator escape check (513 smoke inputs)
     * DONE fuzz_request_state.py — _read_live_value via temp file writes (511 smoke inputs)
     * Note: smoke mode (PYTHONFUZZ_NO_ATHERIS=1) runs in CI; full Atheris requires llvm-enabled Clang on macOS
   * DONE Branch coverage enforcement [2/2]
     * DONE branch = true in [tool.coverage.run]
     * DONE property-tests job contributes .coverage.property to coverage-gate
   * DONE Add pytest-based fuzz-smoke CI job [1/1]
     * DONE fuzz-smoke CircleCI job runs all three harnesses in smoke mode on every push
   * DONE Harden deterministic integration and e2e coverage using Antithesis-style principles [4/4]
     * DONE Shared file-backed storage fake under `test_support/storage_fakes.py` for reproducible whole-workflow tests
     * DONE Adapter + FastAPI service workflow invariants covered end to end (upload/list/info/download/delete/repeated delete)
     * DONE `main.py` subprocess e2e no longer depends on live AWS; fake `aws_client_impl` is injected through `PYTHONPATH`
     * DONE Stronger tests flushed out and fixed adapter boundary bugs: 401 → `AuthenticationError`, container 404 preservation, and `upload_file()` no longer misclassifies remote failures as local file errors
   * DONE Publish repo-local pytest contributor guide and fixture cleanup pass [4/4]
     * DONE Added `tests/README.md` with philosophy, suite map, and examples for writing new tests
     * DONE Consolidated repeated `aws_client_service` endpoint fixtures into a package-local `conftest.py`
     * DONE Removed weaker duplicate auth test file in favor of one stronger source of truth
     * DONE Strengthened `aws_client_service` auth route tests with parametrized auth acceptance/rejection and callback cleanup checks
   * TODO Add pytest-randomly — randomizes test order, catches tests that depend on each other (Sprint 5 Block H)
   * TODO Add mutation testing via mutmut (NOT cosmic-ray) (Sprint 5 Block F) [0/4]
     * TODO Run on auth.py, sessions.py, conversation.py, state_store.py first — smallest, security-critical, has both unit + property tests
     * TODO Goal: zero surviving mutants in those four files; do not try the whole repo
     * TODO Add nox -s mutate session that runs the curated set
     * TODO mutmut chosen over cosmic-ray: better Python 3.12 support, simpler mutant-survival reporting
   * TODO Add snapshot testing via syrupy — captures API response shapes, fails on drift
   * DONE Add BDD via pytest-bdd (Sprint 5 Block E) [5/5]
     * DONE features/chat_turn.feature — signed wrapper request → reply outcome and read-only tool availability
     * DONE features/confirmation_flow.feature — destructive intent → confirmation_required → same-actor completion
     * DONE features/wrapper_signed_auth.feature — missing/invalid/replayed/tampered signature paths
     * DONE Step definitions in tests/bdd/, using deterministic fake AI/storage dependencies
     * DONE Decision: pytest-bdd over behave — stays in pytest, no separate runner, no double-fixture story
   * TODO Add contract testing via Schemathesis (Sprint 5 Block G) [0/3]
     * TODO Schemathesis generates property-based HTTP requests from the FastAPI OpenAPI spec
     * TODO Run against TestClient(app) in CI on every push as schemathesis-tests job
     * TODO Validates response shape against declared schema — catches unintentional drift
   * TODO Add load testing via locust (Sprint 5 Block H) [0/3]
     * TODO Locustfile for /ai/chat/turn with signed-request auth and per-user conversation IDs
     * TODO Headless mode in nox -s load with assertions on p99 latency and error rate
     * TODO Local-only initially; nightly CI later, never on every push
   * TODO Add deterministic time control via freezegun (Sprint 5 Blocks B+C) [0/3]
     * TODO Use for rate-limiter, idempotency TTL, session expiry tests where _now injection is impractical
     * TODO Complements existing _now: float | None = None clock-injection seam — does not replace it
     * TODO Existing seam stays the preferred approach; freezegun fills the gap where threading time through call chains is too invasive
   * TODO Add typed test-data factories via polyfactory (Sprint 5 Block K) [0/4]
     * TODO ChatTurnRequestFactory, MessageRecordFactory, TurnAttachmentFactory, ObjectInfoFactory, S3ClientErrorFactory
     * TODO Migrate hardcoded "test-bucket" / "some-key.txt" / principal IDs to factory output (closes audit gap #3)
     * TODO Pydantic-aware — auto-builds from existing schemas
     * TODO faker only as a polyfactory sub-provider for realism, never standalone in property tests
   * TODO Add micro-benchmarks via pytest-benchmark (Sprint 5 Block H) [0/3]
     * TODO Bench: conversation trim, session JSON serialization, _decoded_base64_size, HMAC sign + verify
     * TODO Fail CI on >15% regression vs. baseline
     * TODO Locust covers macro-load; pytest-benchmark covers micro-perf invariants
   * TODO Build fault injection test harness with deterministic replay (DST) [0/5]
     * TODO Inspired by FoundationDB / Antithesis / WarpStream
     * TODO S3Client takes a Clock dep — never calls time.time() or asyncio.sleep() directly
     * TODO S3Client calls through an ObjectStore abstraction — never calls boto3 directly
     * TODO Single-threaded simulation, controlled fakes, reproducible by seed
     * TODO Simulate: S3 500s mid-multipart, network partitions, partial writes, clock skew
   * TODO Write Hypothesis resume invariant tests [0/2]
     * TODO For any sequence of uploads and failures, resume produces correct final object
     * TODO Or produces a clean abort — never silent corruption
   * TODO Add benchmarks as first-class CI artifacts [0/2]
     * TODO Measure S3 upload/download throughput, fail CI on >10% regression
     * TODO Throughput vs. file size curves in README
   * TODO Concurrency and session isolation tests (ai_server) [0/4]
     * TODO Concurrent requests to the same session race against asyncio.Lock — assert ordered, consistent state
     * TODO Token bucket never exceeds capacity under burst; rejection count is exact
     * TODO Starvation: client that waits the correct duration always succeeds
     * TODO Use pytest-asyncio with multiple concurrent coroutines, not sleep-based sequencing
   * TODO Session persistence and crash-recovery tests [0/3]
     * TODO Simulate crash between tmp write and rename — verify no corrupt session is loaded on restart
     * TODO Corrupt or zero-byte session file on startup returns a clean error, not a 500
     * TODO Concurrent writers on the same session file never interleave — final state is one writer's complete output
   * TODO Auth security regression tests [0/3]
     * TODO Replayed valid signed request is rejected (nonce or timestamp window)
     * TODO Tampered payload returns 401, not 500 — abstraction boundary holds under malformed input
     * TODO Missing or malformed X-API-Key / signing header returns 401 with a useful message
   * TODO Transport failure → domain error mapping [0/3]
     * TODO OpenRouter client: timeout, connection reset, 5xx each map to a specific domain exception
     * TODO S3 client: boto3 transport errors translate to domain exceptions, not raw botocore errors
     * TODO No SDK-internal exception type leaks past the abstraction boundary — assert isinstance on raised type
    * TODO Tool call safety and destructive operation guards [0/3]
      * TODO Delete tool refuses to execute without an explicit confirmation signal in the tool input
      * TODO Malformed tool call (missing required field, wrong type) returns a structured error, not a 500
      * TODO Fuzzing tool input schema — Hypothesis generates random dict payloads, none should panic the handler
    * DONE Audit and improve test quality based on industry best practices [6/6]
      * DONE Review existing tests for clarity, maintainability, and effectiveness
      * DONE Added property-based testing for complex invariants (Conversation, session IDs, auth protocol, rate-limiter)
      * DONE All test files carry pytestmark; no unmarked tests
      * DONE Enhance error condition testing via Hypothesis (invalid IDs, unsafe chars, exhausted buckets, wrong secrets)
      * DONE Fuzz harnesses cover security-relevant deserialisation paths
      * DONE docs/source/testing.md documents how every test category works and how to write new tests
   * TODO OpenAPI contract drift detection via Schemathesis [0/3]
     * TODO Schemathesis generates and sends property-based HTTP requests derived from the FastAPI OpenAPI spec
     * TODO Validates that every response matches the declared schema — catches unintentional response shape changes
     * TODO Run in CI against the running service (TestClient or a local uvicorn instance)
   * TODO Structured log shape and sensitive data tests [0/3]
     * TODO Assert every log line emitted during a request is valid JSON and contains request_id and operation fields
     * TODO Assert no log line contains credential-shaped strings (token, secret, key patterns)
     * TODO Use structlog's CapturingLogger or capsys in tests — do not parse stderr with regex
   * TODO Network fault simulation with Toxiproxy (Shopify) [0/3]
     * TODO Proxy all outbound S3 and OpenRouter calls through Toxiproxy in integration tests
     * TODO Inject: latency spikes, bandwidth limits, connection resets, partial reads
     * TODO Assert retry logic fires correctly and timeouts are honored — not just that errors are caught
   * TODO Testcontainers for high-fidelity integration tests [0/2]
     * TODO Run LocalStack (S3-compatible) in a real Docker container instead of moto for integration tier
     * TODO Higher fidelity than pure mocking — catches bugs moto's behavioral approximation hides
   * TODO Metamorphic testing for storage operations [0/3]
     * TODO Upload A then B, or B then A — list result contains both regardless of order (commutativity)
     * TODO Upload an object, download it, re-upload it — final stored bytes are identical (idempotence)
     * TODO Delete a nonexistent key twice — second call returns the same result as the first (idempotence)
   * TODO Regression corpus and bug reproducer discipline [0/2]
     * TODO Every fixed bug gets a test case committed to tests/regression/ with a comment linking the issue
     * TODO Corpus is run as part of the regression marker suite on every CI pass
   * TODO Flaky test tracking and quarantine [0/2]
     * TODO Add pytest-rerunfailures to CI — tests that fail intermittently are automatically flagged
     * TODO Maintain a quarantine list in pyproject.toml; quarantined tests do not block merges but are tracked
   * TODO Branch coverage enforcement [0/2]
     * TODO Enable --cov-branch in pytest config — line coverage misses untaken else branches
     * TODO Raise threshold to 85% branch coverage before moving to 90%+ line coverage goal
   * TODO Formal load testing setup [0/3]
     * TODO Locust scenarios for upload, download, list, delete under concurrent load
     * TODO Throughput benchmarking: pipeline profiled, bottlenecks documented
     * TODO Throughput vs. file size curves in README
   * TODO Chaos engineering and GameDay exercises [0/3]
     * TODO AWS FIS or Gremlin fault injection against the deployed service — kill a pod, drop the S3 connection
     * TODO Scheduled GameDay runbook: inject faults, verify alerts fire, verify recovery is automatic
     * TODO Not a daily CI test — a quarterly discipline with documented outcomes
   * TODO Shadow traffic replay [0/2]
     * TODO Capture a sanitized sample of production requests and replay them against a staging instance
     * TODO Assert response shape and latency are within bounds — catches regressions that synthetic tests miss
     * TODO Requires a production traffic capture pipeline — defer until service has real traffic volume

** TODO Security & Supply Chain [0/8]
   * TODO Harden supply chain in CI [0/7]
     * TODO GuardDog — detects malicious packages before install
     * TODO Semgrep — static analysis for security patterns
     * TODO truffleHog — secret scanning on every push
     * TODO Safety — checks deps against known CVE databases
     * TODO bandit — static analysis for common Python security vulnerabilities
     * TODO pip-licenses / liccheck — license compliance (no GPL leaking into MIT project)
     * TODO SBOM generation (Software Bill of Materials)
   * TODO Store credentials the right way [0/3]
     * TODO AWS OIDC identity federation — eliminate all long-lived CI secrets
     * TODO Trusted publishing to PyPI via OIDC — no PyPI token stored anywhere
     * TODO pydantic-settings for env var management [0/2]
       * TODO One Settings class at startup, typed, validated, documented
       * TODO Never read os.environ.get() directly — always go through Settings

** TODO Releases & Publishing [2/6]
   * DONE Published to PyPI
   * DONE Published to Nix
   * TODO Formalize release pipeline [0/4]
     * TODO Semantic versioning via hatch-vcs — version tied to git tags automatically
     * TODO Signed releases via Sigstore — artifacts cryptographically signed via OIDC
     * TODO cut tag → CI builds wheel → signs → pushes to PyPI → posts release notes
     * TODO Nightly → Beta → Stable release train tracked in Linear
   * TODO Dependabot or Renovate for automated dependency update PRs
   * TODO uv sync --frozen in CI — install from lockfile exactly, never re-resolve
   * TODO Pin all tool versions in [tool.uv] so CI is reproducible 6 months from now

** TODO S3 Client: Core Interface Design [0/9]
   * TODO Design clean interface/implementation separation [0/4]
     * TODO cloud_storage_client_api: zero third-party dependencies (ABC layer only)
     * TODO aws_client_impl: boto3, botocore — no leakage back into the ABC
     * TODO Enforced by mypy --strict, ruff import boundary rules, machine-checked in CI
     * TODO Follow the encode philosophy: minimal deps, each package does one thing
   * TODO Use attrs for typed domain models [0/4]
     * TODO __slots__ = True by default — lower memory, faster attribute access
     * TODO frozen = True for value objects like ObjectInfo (immutable S3 state snapshots)
     * TODO Validators for runtime invariants mypy can't express (key non-empty, size >= 0)
     * TODO Use cattrs for structured un/serialization at service boundaries
   * TODO Build typed domain exception hierarchy [0/3]
     * TODO Each exception carries exactly the fields needed to diagnose that failure
     * TODO Typed domain exceptions modeled in tests via comprehensive error modeling
     * TODO Circuit breaker state machine: Closed → Open → Half-Open → Closed
   * TODO Non-trivial S3 interface — don't just wrap upload_file / download_file [0/3]
     * TODO Multipart uploads for large files
     * TODO Presigned URL generation
     * TODO Bucket and object metadata management
   * TODO Async multipart upload engine via asyncio [0/4]
     * TODO Bounded concurrency via asyncio.Semaphore
     * TODO Chunk pipelining — read chunk N+1 from disk while uploading chunk N
     * TODO Structured cancellation
     * TODO Consider trio nurseries for atomic multipart completion guarantees
   * TODO Adaptive concurrency control [0/3]
     * TODO Measure actual throughput and error rates in real time
     * TODO Dynamically adjust concurrent chunk uploads (back off on 503s, ramp on clean)
     * TODO Closed-loop controller (PID-lite or token bucket with drain rate feedback)
   * TODO Resumable transfer protocol with local checkpointing [0/4]
     * TODO Persist upload state (upload ID, completed part ETags, byte ranges) atomically
     * TODO Checkpoint write uses write-then-rename to temp file (atomic on POSIX)
     * TODO Recovery path reads checkpoint on startup and resumes in-flight uploads
     * TODO Cleanup logic for orphaned multipart uploads (silent cost accumulation footgun)
   * TODO End-to-end integrity verification with streaming checksums [0/4]
     * TODO Per-chunk: compute MD5 and SHA-256 streaming as bytes flow through
     * TODO Send checksum with the part, verify S3 ETag matches
     * TODO After assembly: verify whole-object checksum
     * TODO Background audit: re-fetch metadata, re-verify against local manifest
   * TODO Prefix-aware key distribution [0/2]
     * TODO KeyStrategy interface: random prefix injection, hash-based, time-bucketed
     * TODO Benchmark throughput under simulated high-concurrency writes

** TODO Observability [3/6]
   * DONE OpenTelemetry instrumentation
   * DONE Datadog for per-endpoint latency and error budgets
   * DONE Sentry for exception traces across deployment stages
   * TODO Jaeger or Prometheus local instance [0/2]
     * TODO Export spans and metrics locally
     * TODO Screenshot in README of a real trace — multipart upload with one retried chunk
   * TODO Per-chunk upload latency, retry counts, bytes transferred, throughput/sec
   * TODO Circuit breaker state exported as an observable metric

** TODO Infrastructure & Deployment [5/8]
   * DONE Deployed on k0s
   * DONE HashiCorp Nomad orchestration
   * DONE Terraform for IaC — nothing touches infrastructure except Terraform
   * DONE Nix flakes for reproducible dev environments and deployment targets
   * DONE AWS OIDC identity federation
   * TODO Kubernetes (k0s) on both Render and Fly.io [0/2]
     * TODO Both expose HTTP ingress — k0s targets either via kubeconfig context
     * TODO AWS OIDC and HashiCorp Vault are complementary, not duplicate
   * TODO HashiCorp Vault for runtime secret distribution [0/2]
     * TODO OIDC federation: CI/CD identity — no stored tokens in pipeline
     * TODO Vault: dynamic credentials and lease rotation for running services
   * TODO Look into uvloop as drop-in asyncio event loop replacement

** TODO Design-Pattern Refactor (Sprint 5 follow-up) [0/6]
   * TODO Read faif/python-patterns and apply, do NOT bulk-import [0/5]
     * TODO Strategy pattern: KeyStrategy (already planned in S3 client backlog), retry-policy strategy in stamina adoption
     * TODO Adapter pattern: aws_client_adapter is already this; document explicitly in service_adapter.py docstring
     * TODO Chain-of-Responsibility: tool-call dispatch in nimbus_runtime — currently a dict; evaluate whether explicit chain helps confirmation precedence
     * TODO Factory pattern: get_client_impl() factory contract is canonical; do not introduce a DI framework that fights with it
     * TODO Null Object pattern (James Shore Nullables): formalize FakeAIClient / FakeStorageClient under tests/nullables/ as the canonical test doubles
   * TODO Anti-pattern audit — what NOT to do from the patterns list [0/3]
     * TODO Singleton: rate-limit dict + nonce cache are de-facto singletons; document boundedness explicitly
     * TODO Service Locator: avoid; the get_client_impl factory is intentional dependency injection, not a locator
     * TODO Active Record: not applicable here; storage and runtime stay separate
   * TODO Apply systems-design lens, not pattern checklist [0/2]
     * TODO Patterns earn their keep when they reduce coupling; reject when they add layers without removing complexity
     * TODO Deep modules / shallow interfaces (Ousterhout) is the priority; pattern fidelity is downstream of that

** TODO Auth, Crypto, Signed Tokens (Sprint 5+ planning) [0/6]
   * TODO itsdangerous — LEARN, do not adopt the dep [0/3]
     * TODO Our auth.py implements the same pattern (HMAC-signed timestamped payload with replay defence) — itsdangerous is the canonical reference
     * TODO Read URLSafeTimedSerializer source for: separator handling, timestamp encoding (b64url), key-derivation salt practice
     * TODO If we ever hand out signed download URLs or session cookies, switch to itsdangerous wholesale rather than rolling our own
   * TODO authlib for OIDC service identity (priority 2.4) [0/3]
     * TODO Use case: workload identity (wrapper -> Nimbus) replaces shared HMAC secret with short-lived signed JWT
     * TODO authlib over oauthlib: authlib is higher-level, has JWT/JWS/JWA/JWE built in; oauthlib is signing-primitives only
     * TODO Trigger condition: when Fly + workload identity provider (or self-hosted issuer) is in place; not before
   * TODO Crypto stack [0/3]
     * TODO Keep stdlib hmac/hashlib for current HMAC-SHA256 signing — correct, audited, no dep risk
     * TODO Add pyca/cryptography ONLY when we need x509/AEAD/asymmetric (i.e. when authlib lands, it pulls cryptography transitively)
     * TODO Skip pycryptodome (legacy fallback), skip bcrypt (we don't store passwords)

** TODO Object diffing (test ergonomics, low priority) [0/2]
   * TODO Add deepdiff ONLY for test-side object equality on conversation snapshots and session round-trips [0/2]
     * TODO Use case: assert_conversations_equal(actual, expected) prints structural diff on failure
     * TODO Decision: deepdiff over dictdiffer (deeper structural comparison), over jsondiff (we compare Python objects not raw JSON), over jsonpatch (RFC 6902 is overkill — we don't apply patches)
   * TODO Skip deepdiff if pytest's built-in assertion-rewriting + repr is sufficient for our payload sizes; revisit when asserting whole nested ChatTurnResult fails to give a useful diff

** TODO AI Agent Layer [4/7]
   * DONE Mistral 7B on Modal as serverless inference endpoint using SGlang/vLLM / llama.cpp
   * DONE Tool-calling routing to live storage operations
   * DONE Hot-swappable AI provider layer via Python ABCs
   * DONE Claude Code, Codex, Ampcode for architectural exploration and regression triag TODOe
   * TypeScript/Ink TUI for chat interface to the storage service [0/3]
     * TODO Natural language → tool-call → storage operation loop
     * TODO "List the last 5 files uploaded", "Upload this as notes.txt"
     * TODO Similar to opencode but purpose-built for this project
   * TODO Write production-grade AGENTS.md [0/5]
     * TODO Architectural layer constraints (what imports what)
     * TODO Tool boundaries and autonomy rules
     * TODO Test philosophy and editing invariants
     * TODO Reduces AI hallucination rate on codebase-specific tasks
     * TODO Inspired by Pyrefly, agents.md spec, Mitchell Hashimoto's Ghostty workflow
   * TODO pyrefly alongside mypy in CI [0/2]
     * TODO Run both, document where they disagree and why
     * TODO Flow-sensitive typing catches narrowing bugs mypy misses

** TODO Nimbus REPL Backlog (HW3 follow-ups) [0/8]
   * TODO readline history support in the REPL (up/down arrow recall, persistent history file)
   * TODO Streaming + tool-calling combined parser (reassemble tool-call JSON across chunks by tool_call.id)
   * TODO Rich TUI / syntax highlighting for the REPL (looks cool but adds a big dep)
   * TODO Promote cloud_storage_tools into an MCP server so any MCP-capable client can use it
   * TODO Per-session rate limiting once the Slack/Discord frontend exists (max N delete/min per user)
   * TODO Circuit breaker for OpenRouter via stamina (NOT raw tenacity) [0/4]
     * TODO stamina = opinionated wrapper around tenacity; sane defaults (exponential + jitter), better ergonomics
     * TODO Open after N consecutive failures, half-open probe, close on success
     * TODO Replace ad-hoc retry in _run_with_fallback once Block C lands typed exception classes
     * TODO Decision: stamina over raw tenacity (Hynek wrapper, batteries-included); skip backoff (no docs, less active)
   * TODO Pluggable conversation/state store (JSON on disk today; Valkey later — see Shared-State Backend section) for cross-process persistence
   * TODO Auto-model discovery via GET /api/v1/models with filters (free, context length, tool support)

** TODO Shared-State Backend (priority 2.3 in NIMBUS_NEXT_TODOS) [0/4]
   * TODO Choose Valkey (NOT Redis 8.x or DragonflyDB) when multi-replica deployment lands [0/3]
     * TODO Valkey: BSD 3-clause, Linux Foundation-backed, drop-in for redis-py, already in AWS Elasticache Serverless / GCP Memorystore / Akamai
     * TODO Redis 8.x: AGPLv3 — strong copyleft, license uncertainty after the SSPL fiasco; avoid for shared infra
     * TODO DragonflyDB: BSL (not OSI open-source), C++ from scratch, faster but locks us into commercial terms; only worth it at 20-node Redis cluster scale
   * TODO Use cases for the backend [0/4]
     * TODO Shared idempotency state across replicas (currently file-backed, single-machine)
     * TODO Shared signed-request nonce replay protection (currently file-backed)
     * TODO Slack event-ID dedupe cache (when wrapper goes multi-instance)
     * TODO Optional rate-limit token bucket (currently in-process dict, bounded by user count)
   * TODO Trigger conditions: deploy multi-replica OR run multiple wrapper instances. Until then, single-replica file state is correct.
   * TODO Local-cache alternatives evaluated: diskcache rejected for runtime state (cache semantics ≠ state semantics). diskcache OK only for the model-discovery list (priority 5.2) where staleness is acceptable.

** TODO Load Testing [1/2]
   * DONE Locust load test: 250 concurrent users, 1,200 RPS, p99 < 95ms
   * TODO Formal load testing scenarios — see Testing section above

** HOLD Formal Methods — Stretch [0/3]
   * TODO Write formal consistency model document [0/3]
     * TODO What does the client promise given S3's strong read-after-write consistency?
     * TODO What happens if a PUT succeeds but process dies before recording the ETag?
     * TODO Can two concurrent writers on the same key corrupt each other's uploads?
   * TODO Encode as TLA+ or Alloy model with checked invariants [0/2]
     * TODO Even one checked invariant signals rigor almost no student project has
     * TODO Reference: "Using Lightweight Formal Methods to Validate a Key-Value Storage Node in Amazon S3"
   * TODO Reference: "How Amazon Web Services Uses Formal Methods" (Newcombe, Brooker et al., CACM 2015)

------------------------------------------------------------

* TODO Later [0/14]
** TODO Reading & Research [0/14]
   * TODO Marc Brooker's blog — read after each corresponding build [0/4]
     * TODO Timeouts, Retries, and Backoff with Jitter → after building retry logic
     * TODO It's About Time → after building the Clock dependency injection
     * TODO Learning to Build Distributed Systems → context for the whole project
     * TODO Systems Correctness Practices at AWS (ACM) → before DST harness
   * TODO WarpStream DST blog post → before building fault injection harness
   * TODO "Millions of Tiny Databases" (Brooker et al., NSDI 2020) → Physalia correctness
   * TODO "Using Lightweight Formal Methods..." (Amazon S3 KV node) → before formal spec
   * TODO Fly.io Gossip Glomers → all challenges in order, start after HW ships
   * TODO Hypothesis stateful testing docs → before writing property-based resume tests
   * TODO Google Python Style Guide
   * TODO Open-source models: Mistral, Kimi, and other self-hostable options
   * TODO Clean interface design — low surface area, high functionality
   * TODO API design crash course
   * TODO Resilience patterns: retries, rate-limit handling, idempotency
   * TODO Grokking Concurrency (Bobrov) → before writing the async multipart engine
   * TODO Python Concurrency with asyncio (Fowler, Manning) → bounded semaphores chapter

------------------------------------------------------------

* TODO Brainstorm [0/14]
** TODO Performance [0/4]
   * TODO Profile upload/download pipeline — find actual bottlenecks before optimizing
   * TODO C extension or uvloop to push async throughput past Python's ceiling
   * TODO Benchmark naive boto3 default vs. async engine on files >100MB
   * TODO Measure 2x throughput improvement and put the curve in the README

** TODO Resilience [0/4]
   * TODO What happens when 3 of 8 chunks succeed and then you lose network connectivity?
   * TODO Local work queue with bounded concurrency, backpressure, and cancellation support
   * TODO Checkpoint progress locally — resumable from exact byte offset, not from scratch
   * TODO Exponential backoff with full jitter — not naive exponential (thundering herd)

** TODO Interface Design [0/3]
   * TODO KeyStrategy — pluggable sharding schemes as a first-class interface
   * TODO ObjectStore abstraction — injectable, mockable, swappable in tests
   * TODO Clock abstraction — injectable for deterministic simulation

** TODO Future Integrations [0/3]
   * TODO GCP Cloud Storage backend behind the same ABC
   * TODO OpenTelemetry → Grafana stack for self-hosted observability
   * TODO Locust → continuous performance gate in CI, not just a one-time test

------------------------------------------------------------

* Archive
** DONE Sprint 1 — Core Library
   * DONE Provider-agnostic storage ABC with dependency injection
   * DONE attrs-backed typed domain models with custom exception hierarchy
   * DONE mypy --strict + ruff enforced in CI, zero implementation leakage

** DONE Sprint 2 — Service Layer
   * DONE OAuth 2.0 Authorization Code Flow
   * DONE FastAPI service orchestrated on k0s via HashiCorp Nomad
   * DONE Shared interface versioned as a uv git dependency across 3 teams

** DONE Sprint 3 — AI + Infra
   * DONE Mistral 7B on Modal — serverless inference endpoint with tool-calling
   * DONE Terraform IaC, Nix flakes, AWS OIDC — zero long-lived secrets
   * DONE Locust: 250 concurrent users, 1,200 RPS, p99 < 95ms
   * DONE OpenTelemetry + Datadog + Sentry observability
   * DONE Published to PyPI and Nix
   * DONE Sphinx documentation with autoapi

** DONE Sprint 4 — Test Infrastructure
   * DONE 38 Hypothesis property-based tests: Conversation invariants (stateful machine), session ID validation and file-stem derivation, HMAC-SHA256 signing protocol self-consistency + sensitivity + secret isolation, ChatTurnRequest validation, rate-limiter token-bucket arithmetic
   * DONE 3 Atheris fuzz harnesses (smoke mode): conversation deserialisation, session-ID path-escape, request-state deserialisation — all three in CI under fuzz-smoke job
   * DONE property-tests CircleCI job (branch-coverage-contributing, wired into coverage-gate)
   * DONE fuzz-smoke CircleCI job (runs on every push, no Atheris dependency required)
   * DONE All test files tagged with pytestmark — no unmarked tests remain
   * DONE unit-tests CI job filtered to -m "unit or regression" to prevent bleed-through
   * DONE docs/source/testing.md — complete testing guide added to Sphinx index
   * DONE CONTRIBUTING.md updated: five test layers, fuzz smoke commands, expanded marker table, updated CI job table
   * DONE fuzz/README.md: Linux/macOS setup, smoke mode, corpus usage, crash reproduction, what each harness checks
