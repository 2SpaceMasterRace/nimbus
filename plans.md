#+title: plans.md
#+subtitle: Inspired by John Carmack's .plan files

* Prefix legend
  * DONE  completed
  * TODO  not yet started
  * NOW   in progress
  * HOLD  blocked / on ice
  * KILL  decided against

------------------------------------------------------------
 migrate to buildkite, launchdarkly, temporal, Also add to observability tasks : run tests, review telemetry, and query feature flags.
integrate cloudflare and other protection against the publicly deployed servers
ngrok too

* TODO Now [2/20]
** DONE Documentation [2/8]
   * DONE Setup Sphinx similar to attrs documentation
   * DONE Add proper git-workflow (assignment-amend) to CONTRIBUTION.md
   * TODO Write a proper MkDocs / Sphinx site [0/3]
     * TODO Quickstart guide
     * TODO Architecture overview explaining interface/implementation separation
     * TODO API reference docs auto-generated from docstrings
   * TODO Add sphinx-multiversion to host multiple doc versions simultaneously
   * TODO Add doctest integration — examples in docstrings are executable tests in CI
   * TODO Enforce Google-style docstrings via ruff D-rules (pydocstyle)
   * TODO Add CHANGELOG.md generated by Towncrier [0/3]
     * TODO Every PR adds a news fragment in changelog.d/
     * TODO Release cuts the full changelog automatically
     * TODO Ties directly into the Nightly → Beta → Stable release train
   * TODO Write governance and policy docs [0/3]
     * TODO SECURITY.md — responsible disclosure policy
     * TODO GOVERNANCE.md — who can merge, who can release, how decisions get made
     * TODO CITATION.cff — for academic use

** DONE Tooling & Codebase Hygiene [2/9]
   * DONE Add structured logging via structlog [0/2]
     * DONE JSON log lines with bound context (request ID, bucket name, operation type)
     * DONE Every log line machine-parseable and correlatable with OTel traces by request ID
   * DONE Setup CircleCI [0/4]
     * DONE Linting (ruff)
     * DONE Type checking (mypy --strict)
     * DONE Tests + coverage reporting (90%+ threshold)
     * DONE pre-commit run --all-files as a fast gate before slow test jobs
   * TODO Setup pre-commit hooks [0/6]
     * TODO trailing-whitespace, end-of-file-fixer, check-yaml, check-toml, check-ast
     * TODO check-merge-conflict, debug-statements (from pre-commit-hooks)
     * TODO pyupgrade --py312-plus (auto-upgrades syntax: Union[x,y] → x|y, etc.)
     * TODO add-trailing-comma (cleaner diffs when adding function args)
     * TODO ruff-check --fix + ruff-format
     * TODO mypy --strict with pass_filenames: false (catches cross-file type errors)
   * TODO Add py.typed marker file to signal typed package to downstream consumers
   * TODO Add nox / tox for task automation [0/2]
     * TODO nox for Python-native lint, test, docs, security sessions
     * TODO tox for multi-Python-version matrix (3.11, 3.12, 3.13)
   * TODO Setup justfile as single canonical entry point for all dev commands [0/2]
     * TODO just setup, just test, just lint — new contributor needs nothing else
     * TODO install, format, test, lint, docs, security targets all covered
   * TODO Look into Graphite for the team — stacked diffs and PR workflow
   * TODO Setup issue triage workflow similar to Ghostty [0/3]
     * TODO CODEOWNERS — specific files owned by specific people, auto-assigned as reviewers
     * TODO Issue templates — bug report, feature request, structured for fast triage
     * TODO github-labeler — auto-tags PRs by files changed
   * TODO Add Hurl for HTTP request testing and CI integration [0/4]
     * TODO Write .hurl files for every FastAPI endpoint (upload, download, list, delete)
     * TODO Run hurl --test in CI as a lightweight integration smoke layer
     * TODO Use Hurl for contract-level testing against the live Fly.io service
     * TODO Replace manual curl one-liners in CONTRIBUTING.md with runnable .hurl files

** TODO Testing [1/10]
   * DONE Look into mocks for testing (moto for AWS)
   * TODO Write genuinely good tests with realistic mocking via moto [0/2]
     * TODO unit, integration, and property-based test tiers
     * TODO meaningful coverage threshold (90%+) enforced in CI
   * TODO Setup Hypothesis for property-based testing [0/3]
     * TODO Generate adversarial S3 key names (Unicode, empty strings, 1024-byte limit, null bytes)
     * TODO Generate sequences of operations (put, get, delete, list) and verify consistency
     * TODO Stateful testing — verify invariants across arbitrary input spaces
   * TODO Setup pytest markers: unit, integration, property, regression, e2e
   * TODO Add pytest-randomly — randomizes test order, catches tests that depend on each other
   * TODO Add mutation testing via mutmut or cosmic-ray [0/2]
     * TODO Introduces deliberate bugs and checks your tests catch them
     * TODO Forces tests that actually assert things, not just run
   * TODO Add snapshot testing via syrupy — captures API response shapes, fails on drift
   * TODO Add contract testing via pact [0/2]
     * TODO Verifies adapter matches what other teams' services actually return
     * TODO No need for their service to be running
   * TODO Build fault injection test harness with deterministic replay (DST) [0/5]
     * TODO Inspired by FoundationDB / Antithesis / WarpStream
     * TODO S3Client takes a Clock dep — never calls time.time() or asyncio.sleep() directly
     * TODO S3Client calls through an ObjectStore abstraction — never calls boto3 directly
     * TODO Single-threaded simulation, controlled fakes, reproducible by seed
     * TODO Simulate: S3 500s mid-multipart, network partitions, partial writes, clock skew
   * TODO Write Hypothesis resume invariant tests [0/2]
     * TODO For any sequence of uploads and failures, resume produces correct final object
     * TODO Or produces a clean abort — never silent corruption
   * TODO Add benchmarks as first-class CI artifacts [0/2]
     * TODO Measure S3 upload/download throughput, fail CI on >10% regression
     * TODO Throughput vs. file size curves in README

** TODO Security & Supply Chain [0/8]
   * TODO Harden supply chain in CI [0/7]
     * TODO GuardDog — detects malicious packages before install
     * TODO Semgrep — static analysis for security patterns
     * TODO truffleHog — secret scanning on every push
     * TODO Safety — checks deps against known CVE databases
     * TODO bandit — static analysis for common Python security vulnerabilities
     * TODO pip-licenses / liccheck — license compliance (no GPL leaking into MIT project)
     * TODO SBOM generation (Software Bill of Materials)
   * TODO Store credentials the right way [0/3]
     * TODO AWS OIDC identity federation — eliminate all long-lived CI secrets
     * TODO Trusted publishing to PyPI via OIDC — no PyPI token stored anywhere
     * TODO pydantic-settings for env var management [0/2]
       * TODO One Settings class at startup, typed, validated, documented
       * TODO Never read os.environ.get() directly — always go through Settings

** TODO Releases & Publishing [2/6]
   * DONE Published to PyPI
   * DONE Published to Nix
   * TODO Formalize release pipeline [0/4]
     * TODO Semantic versioning via hatch-vcs — version tied to git tags automatically
     * TODO Signed releases via Sigstore — artifacts cryptographically signed via OIDC
     * TODO cut tag → CI builds wheel → signs → pushes to PyPI → posts release notes
     * TODO Nightly → Beta → Stable release train tracked in Linear
   * TODO Dependabot or Renovate for automated dependency update PRs
   * TODO uv sync --frozen in CI — install from lockfile exactly, never re-resolve
   * TODO Pin all tool versions in [tool.uv] so CI is reproducible 6 months from now

** TODO S3 Client: Core Interface Design [0/9]
   * TODO Design clean interface/implementation separation [0/4]
     * TODO cloud_storage_client_api: zero third-party dependencies (ABC layer only)
     * TODO aws_client_impl: boto3, botocore — no leakage back into the ABC
     * TODO Enforced by mypy --strict, ruff import boundary rules, machine-checked in CI
     * TODO Follow the encode philosophy: minimal deps, each package does one thing
   * TODO Use attrs for typed domain models [0/4]
     * TODO __slots__ = True by default — lower memory, faster attribute access
     * TODO frozen = True for value objects like ObjectInfo (immutable S3 state snapshots)
     * TODO Validators for runtime invariants mypy can't express (key non-empty, size >= 0)
     * TODO Use cattrs for structured un/serialization at service boundaries
   * TODO Build typed domain exception hierarchy [0/3]
     * TODO Each exception carries exactly the fields needed to diagnose that failure
     * TODO Typed domain exceptions modeled in tests via comprehensive error modeling
     * TODO Circuit breaker state machine: Closed → Open → Half-Open → Closed
   * TODO Non-trivial S3 interface — don't just wrap upload_file / download_file [0/3]
     * TODO Multipart uploads for large files
     * TODO Presigned URL generation
     * TODO Bucket and object metadata management
   * TODO Async multipart upload engine via asyncio [0/4]
     * TODO Bounded concurrency via asyncio.Semaphore
     * TODO Chunk pipelining — read chunk N+1 from disk while uploading chunk N
     * TODO Structured cancellation
     * TODO Consider trio nurseries for atomic multipart completion guarantees
   * TODO Adaptive concurrency control [0/3]
     * TODO Measure actual throughput and error rates in real time
     * TODO Dynamically adjust concurrent chunk uploads (back off on 503s, ramp on clean)
     * TODO Closed-loop controller (PID-lite or token bucket with drain rate feedback)
   * TODO Resumable transfer protocol with local checkpointing [0/4]
     * TODO Persist upload state (upload ID, completed part ETags, byte ranges) atomically
     * TODO Checkpoint write uses write-then-rename to temp file (atomic on POSIX)
     * TODO Recovery path reads checkpoint on startup and resumes in-flight uploads
     * TODO Cleanup logic for orphaned multipart uploads (silent cost accumulation footgun)
   * TODO End-to-end integrity verification with streaming checksums [0/4]
     * TODO Per-chunk: compute MD5 and SHA-256 streaming as bytes flow through
     * TODO Send checksum with the part, verify S3 ETag matches
     * TODO After assembly: verify whole-object checksum
     * TODO Background audit: re-fetch metadata, re-verify against local manifest
   * TODO Prefix-aware key distribution [0/2]
     * TODO KeyStrategy interface: random prefix injection, hash-based, time-bucketed
     * TODO Benchmark throughput under simulated high-concurrency writes

** TODO Observability [3/6]
   * DONE OpenTelemetry instrumentation
   * DONE Datadog for per-endpoint latency and error budgets
   * DONE Sentry for exception traces across deployment stages
   * TODO Jaeger or Prometheus local instance [0/2]
     * TODO Export spans and metrics locally
     * TODO Screenshot in README of a real trace — multipart upload with one retried chunk
   * TODO Per-chunk upload latency, retry counts, bytes transferred, throughput/sec
   * TODO Circuit breaker state exported as an observable metric

** TODO Infrastructure & Deployment [5/8]
   * DONE Deployed on k0s
   * DONE HashiCorp Nomad orchestration
   * DONE Terraform for IaC — nothing touches infrastructure except Terraform
   * DONE Nix flakes for reproducible dev environments and deployment targets
   * DONE AWS OIDC identity federation
   * TODO Kubernetes (k0s) on both Render and Fly.io [0/2]
     * TODO Both expose HTTP ingress — k0s targets either via kubeconfig context
     * TODO AWS OIDC and HashiCorp Vault are complementary, not duplicate
   * TODO HashiCorp Vault for runtime secret distribution [0/2]
     * TODO OIDC federation: CI/CD identity — no stored tokens in pipeline
     * TODO Vault: dynamic credentials and lease rotation for running services
   * TODO Look into uvloop as drop-in asyncio event loop replacement

** TODO AI Agent Layer [4/7]
   * DONE Mistral 7B on Modal as serverless inference endpoint using SGlang/vLLM / llama.cpp
   * DONE Tool-calling routing to live storage operations
   * DONE Hot-swappable AI provider layer via Python ABCs
   * DONE Claude Code, Codex, Ampcode for architectural exploration and regression triage
   * TODO TypeScript/Ink TUI for chat interface to the storage service [0/3]
     * TODO Natural language → tool-call → storage operation loop
     * TODO "List the last 5 files uploaded", "Upload this as notes.txt"
     * TODO Similar to opencode but purpose-built for this project
   * TODO Write production-grade AGENTS.md [0/5]
     * TODO Architectural layer constraints (what imports what)
     * TODO Tool boundaries and autonomy rules
     * TODO Test philosophy and editing invariants
     * TODO Reduces AI hallucination rate on codebase-specific tasks
     * TODO Inspired by Pyrefly, agents.md spec, Mitchell Hashimoto's Ghostty workflow
   * TODO pyrefly alongside mypy in CI [0/2]
     * TODO Run both, document where they disagree and why
     * TODO Flow-sensitive typing catches narrowing bugs mypy misses

** TODO Nimbus REPL Backlog (HW3 follow-ups) [0/8]
   * TODO readline history support in the REPL (up/down arrow recall, persistent history file)
   * TODO Streaming + tool-calling combined parser (reassemble tool-call JSON across chunks by tool_call.id)
   * TODO Rich TUI / syntax highlighting for the REPL (looks cool but adds a big dep)
   * TODO Promote cloud_storage_tools into an MCP server so any MCP-capable client can use it
   * TODO Per-session rate limiting once the Slack/Discord frontend exists (max N delete/min per user)
   * TODO Circuit breaker for OpenRouter: open after N consecutive failures, half-open probe, close on success
   * TODO Pluggable conversation store (JSON on disk today, Redis later) for cross-process persistence
   * TODO Auto-model discovery via GET /api/v1/models with filters (free, context length, tool support)

** TODO Load Testing [1/3]
   * DONE Locust load test: 250 concurrent users, 1,200 RPS, p99 < 95ms
   * TODO Formal load testing setup [0/3]
     * TODO Locust scenarios for upload, download, list, delete under concurrent load
     * TODO Throughput benchmarking: pipeline profiled, bottlenecks documented
     * TODO Throughput vs. file size curves in README

** HOLD Formal Methods — Stretch [0/3]
   * TODO Write formal consistency model document [0/3]
     * TODO What does the client promise given S3's strong read-after-write consistency?
     * TODO What happens if a PUT succeeds but process dies before recording the ETag?
     * TODO Can two concurrent writers on the same key corrupt each other's uploads?
   * TODO Encode as TLA+ or Alloy model with checked invariants [0/2]
     * TODO Even one checked invariant signals rigor almost no student project has
     * TODO Reference: "Using Lightweight Formal Methods to Validate a Key-Value Storage Node in Amazon S3"
   * TODO Reference: "How Amazon Web Services Uses Formal Methods" (Newcombe, Brooker et al., CACM 2015)

------------------------------------------------------------

* TODO Later [0/14]
** TODO Reading & Research [0/14]
   * TODO Marc Brooker's blog — read after each corresponding build [0/4]
     * TODO Timeouts, Retries, and Backoff with Jitter → after building retry logic
     * TODO It's About Time → after building the Clock dependency injection
     * TODO Learning to Build Distributed Systems → context for the whole project
     * TODO Systems Correctness Practices at AWS (ACM) → before DST harness
   * TODO WarpStream DST blog post → before building fault injection harness
   * TODO "Millions of Tiny Databases" (Brooker et al., NSDI 2020) → Physalia correctness
   * TODO "Using Lightweight Formal Methods..." (Amazon S3 KV node) → before formal spec
   * TODO Fly.io Gossip Glomers → all challenges in order, start after HW ships
   * TODO Hypothesis stateful testing docs → before writing property-based resume tests
   * TODO Google Python Style Guide
   * TODO Open-source models: Mistral, Kimi, and other self-hostable options
   * TODO Clean interface design — low surface area, high functionality
   * TODO API design crash course
   * TODO Resilience patterns: retries, rate-limit handling, idempotency
   * TODO Grokking Concurrency (Bobrov) → before writing the async multipart engine
   * TODO Python Concurrency with asyncio (Fowler, Manning) → bounded semaphores chapter

------------------------------------------------------------

* TODO Brainstorm [0/14]
** TODO Performance [0/4]
   * TODO Profile upload/download pipeline — find actual bottlenecks before optimizing
   * TODO C extension or uvloop to push async throughput past Python's ceiling
   * TODO Benchmark naive boto3 default vs. async engine on files >100MB
   * TODO Measure 2x throughput improvement and put the curve in the README

** TODO Resilience [0/4]
   * TODO What happens when 3 of 8 chunks succeed and then you lose network connectivity?
   * TODO Local work queue with bounded concurrency, backpressure, and cancellation support
   * TODO Checkpoint progress locally — resumable from exact byte offset, not from scratch
   * TODO Exponential backoff with full jitter — not naive exponential (thundering herd)

** TODO Interface Design [0/3]
   * TODO KeyStrategy — pluggable sharding schemes as a first-class interface
   * TODO ObjectStore abstraction — injectable, mockable, swappable in tests
   * TODO Clock abstraction — injectable for deterministic simulation

** TODO Future Integrations [0/3]
   * TODO GCP Cloud Storage backend behind the same ABC
   * TODO OpenTelemetry → Grafana stack for self-hosted observability
   * TODO Locust → continuous performance gate in CI, not just a one-time test

------------------------------------------------------------

* Archive
** DONE Sprint 1 — Core Library
   * DONE Provider-agnostic storage ABC with dependency injection
   * DONE attrs-backed typed domain models with custom exception hierarchy
   * DONE mypy --strict + ruff enforced in CI, zero implementation leakage

** DONE Sprint 2 — Service Layer
   * DONE OAuth 2.0 Authorization Code Flow
   * DONE FastAPI service orchestrated on k0s via HashiCorp Nomad
   * DONE Shared interface versioned as a uv git dependency across 3 teams

** DONE Sprint 3 — AI + Infra
   * DONE Mistral 7B on Modal — serverless inference endpoint with tool-calling
   * DONE Terraform IaC, Nix flakes, AWS OIDC — zero long-lived secrets
   * DONE Locust: 250 concurrent users, 1,200 RPS, p99 < 95ms
   * DONE OpenTelemetry + Datadog + Sentry observability
   * DONE Published to PyPI and Nix
   * DONE Sphinx documentation with autoapi
